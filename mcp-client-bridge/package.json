{
  "name": "@orchestrator/mcp-bridge",
  "version": "1.0.0",
  "description": "MCP Client Bridge — connects to MCP Orchestrator via HTTP Streamable transport",
  "main": "dist/index.js",
  "bin": {
    "mcp-bridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "lint": "eslint src/",
    "test": "jest"
  },
  "keywords": ["mcp", "bridge", "orchestrator", "ai", "tools"],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
